<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <!-- Multi - Company Rules -->
    <record id="limousine_pricing_comp_rule" model="ir.rule">
        <field name="name">Limousine Pricing multi-company</field>
        <field name="model_id" ref="model_limousine_pricing"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
    </record>

    <record id="limousine_pricing_line_comp_rule" model="ir.rule">
        <field name="name">Limousine Pricing Line multi-company</field>
        <field name="model_id" ref="model_limousine_pricing_line"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
    </record>

    <record id="limousine_fines_config_comp_rule" model="ir.rule">
        <field name="name">Limousine Fines Config multi-company</field>
        <field name="model_id" ref="model_limousine_fines_config"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
    </record>

    <record id="limousine_contract_comp_rule" model="ir.rule">
        <field name="name">Limousine Contract multi-company</field>
        <field name="model_id" ref="model_limousine_contract"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
    </record>

    <record id="limousine_contract_timesheet_comp_rule" model="ir.rule">
        <field name="name">Limousine Contract Timesheet multi-company</field>
        <field name="model_id" ref="model_limousine_contract_timesheet"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
    </record>

</odoo>
